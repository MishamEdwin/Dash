	• This is my excel table's first row.
	• The formula for K2=D2/E2, L2 =MAX(0, ROUNDUP(((G2/0.95)-D2)/K2,0)), M2=K2*L2, N2 =D2+M2, O2 =G2/N2%.
	• Right now the "Target NOP" gives a value so that the NEW LR is lower than 95%. I want to add a new column called "Target GWP" which gives a value so that the NEW LR is lower than 95%.
	• Both the "Target NOP" and "Target GWP" should work in sync. 
	• By default they both should display an automated appropriate value.
	• If I manually enter a value for "Target NOP" then the "Target GWP" value should be updated so that "NEW LR" is lower than 95%.
If I manually enter a value for "Target GWP" then the "Target NOP" value should be updated so that "NEW LR" is lower than 95%.
